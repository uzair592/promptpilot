# Question Lifecycle

Questions move from `generated` to `presented`, then `answered` or `skipped`. A question whose gap is resolved is not presented again. Sessions complete when no unresolved gaps remain.
