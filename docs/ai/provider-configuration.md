# Provider Configuration

PromptPilot starts in baseline fallback mode when provider variables are missing. OpenRouter is selected through configuration, not domain code. Never commit `.env` or API keys. Use a fixed model, analyzer version, dataset version, mode, and timestamp when recording evaluation results.
