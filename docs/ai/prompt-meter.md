# Prompt Meter

The Prompt Meter presents an analysis as `Poor` (0-49), `Medium` (50-79), or `Good` (80-100). These are initial heuristics for the FYP and require empirical validation. Users explicitly request analysis from a user message; no automatic interruption is added.
