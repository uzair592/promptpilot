# Context Provenance

Each chunk records its document, chunk order, processing version, and parser provenance such as text or CSV row range. Source files remain in local object-style storage and database rows retain metadata and checksums.
