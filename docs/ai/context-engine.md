# Context Engine

The initial Context Engine slice stores validated project documents and deterministic extracted chunks with provenance. Project Memory remains separate structured metadata. Parser, retrieval, and assembly boundaries are reserved for the next increment.
