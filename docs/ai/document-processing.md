# Document Processing

Documents use an uploaded, processing, processed, or failed lifecycle. The current baseline safely supports TXT and CSV extraction plus image metadata storage. PDF, DOCX, and XLSX parser adapters remain explicit future work; unsupported formats fail without creating fake chunks.
