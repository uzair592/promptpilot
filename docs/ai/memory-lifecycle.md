# Memory Lifecycle

Memory items are `active`, `superseded`, or `archived`. Adding a new user value for the same project subject supersedes the prior active value without deleting it. User provenance is never replaced by an AI inference.
